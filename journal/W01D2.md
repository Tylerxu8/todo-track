# Week 1 Day 2

## What does `type="submit"` on the button actually cause to happen? What did you observe when you changed it to `type="button"`?

`type="submit"` is making the `Add` button clickable instead of just pressing `Enter`. 

When I changed it to `type="button"`, the `Add` button does nothing and the only way to submit is by pressing `Enter`.

## When you pressed Enter and the page reloaded with `?task=buy+milk` in the URL - describe, step by step in your own words, what the browser did

When the page reloaded with `?task=buy+milk`, this happened because the browser collected the fields, appeneded them to the URL, and reloaded the page.

## Why must the sample `<li>` be deleted on Day 5, and what bug would it cause if you forgot?

The sample has to be deleted becaue it is just a place holder for what the app could look like so if I didn't delete the sample, it would stay there forever.

## Pick one line or attribute you typed today that you couldn't have written from scratch. Name it and the part you can't explain

I would say the whole `<ul>` structure because this is my first time doing anything like this and it is still very confusing.

