# Week 1 Day 4

## After you added `box-sizing: border-box`, what specifically changed about how `width` and `padding` interact? Use the input box as your example

When adding `border-box` every element is set to 200px wide and the padding eats inward.

## `#task-input { flex: 1 }` and `#list li span { flex: 1 }` - explain in plain words what `flex: 1` is doing in each place.

`flex: 1` is saying to take all the leftover horizantal space in the row.

## When you removed `#list li span { flex: 1 }` and the Delete button jumped, what does that tell you about how the row was being held apart?

The row is being held apart with `flex: 1` which is taking up all the left over space on the left which pushes the Delete button to the right
